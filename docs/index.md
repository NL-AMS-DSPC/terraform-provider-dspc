---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "asc Provider"
description: |-
  The ASC provider manages virtual machines, containers, and storage resources across different platforms.
---

# asc Provider

The ASC provider manages virtual machines, containers, and storage resources across different platforms.

## Example Usage

```terraform
terraform {
  required_providers {
    asc = {
      source  = "asc/asc"
      version = "~> 1.0"
    }
  }
}

provider "asc" {
  # REQUIRED: Configure via environment variables (recommended for CI/CD)
  # ASC_ENDPOINT="https://api.example.com"
  # ASC_NAMESPACE="corp-namespace"
  # ASC_USERNAME="auth-service-client-id"
  # ASC_PASSWORD="auth-service-client-secret"
  # ASC_AUTH_URL="https://auth-service.example.com"
  # ASC_ORG="organization-realm"
  # ASC_TIMEOUT="60"  # Optional, defaults to 30

  # OR configure directly (not recommended for production)
  # endpoint  = "https://api.example.com"               # REQUIRED
  # username  = "auth-service-client-id"                # REQUIRED (Auth service client ID)
  # password  = "auth-service-client-secret"            # REQUIRED (Auth service client secret)
  # auth_url  = "https://auth-service.example.com"      # REQUIRED (Auth service base URL)
  # org       = "organization-realm"                    # REQUIRED (Auth service realm)
  # namespace = "corp-namespace"                        # REQUIRED
  # timeout   = 60                                      # Optional, defaults to 30
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `auth_url` (String) Authentication service URL. Required - can be set via provider config or ASC_AUTH_URL environment variable.
- `endpoint` (String) The endpoint URL for the ASC VM Deployer API. Required - can be set via provider config or ASC_ENDPOINT environment variable.
- `namespace` (String) The name of the namespace where the VM is deployed.
- `org` (String) Organization for authentication. Required - can be set via provider config or ASC_ORG environment variable.
- `password` (String, Sensitive) Password for authentication. Required - can be set via provider config or ASC_PASSWORD environment variable.
- `timeout` (Number) The timeout in seconds for API requests. Defaults to 30.
- `username` (String) Username for authentication. Required - can be set via provider config or ASC_USERNAME environment variable.
