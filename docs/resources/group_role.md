---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "dspc_group_role Resource - dspc"
subcategory: ""
description: |-
  Manages the assignment of a role to a authorization group.
---

# dspc_group_role (Resource)

Manages the assignment of a role to a authorization group.

## Example Usage

```terraform
# Create a role and a group, then assign the role to the group
resource "dspc_role" "example" {
  name = "vm-operator"
  actions = [
    "vm:CreateVM",
    "vm:DeleteVM",
    "vm:ListVMs",
  ]
}

resource "dspc_group" "example" {
  name = "platform-team"
}

resource "dspc_group_role" "example" {
  group_name = dspc_group.example.name
  role_name  = dspc_role.example.name
}

# Output the assignment details
output "group_role_id" {
  description = "The ID of the group role assignment"
  value       = dspc_group_role.example.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `group_name` (String) The name of the group.
- `role_name` (String) The name of the role to assign to the group.

### Read-Only

- `id` (String) The unique identifier for this assignment.
